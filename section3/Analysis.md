# Analysis Before Designing

## Section Overview

It is a section which has plain text with font-family **"Inter"**, a `h1` part and a paragraph along with a button underneath. On the right it has a **3D robo/chat-bot object**.

## Color & Background

It has a **deep navy color background** with different gradients and degrees of color.

## Design Challenges

The designing colour would be the challenging part — the color gradients of the **chatbot section** is much tougher than the content section, since it has different shapes and colors of different degrees.
facing challenges creating **"SVG"** elements as shown in the section, it has very complicated 'svg' fomatting so hoping to learn and finish it as soon as possible.

## Learnings:

Some of the learnings while developing this page were how to use **SVG's** and exporting them from figma to my own **IDE** environment, later putting them together to achieve the objective

learn to add media queries for every class that have been created. The rest is as usual only difficult part was the **SVG** developing section.